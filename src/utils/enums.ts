export enum EButton {
   RecordVariant = "text",
   RecordDensity = "comfortable"
}

export enum ENotify {
   Success = "success",
   Error = "error",
   Danger = "danger",
   Warning = "warning"
}

export enum EUser {
   "user" = 300,
   "admin" = 400
}

export enum ELanguage {
   "tr-TR" = 1,
   "en-US" = 2
}
